# --- Local development (FastAPI on :8000) ---
NEXT_PUBLIC_API_BASE=http://127.0.0.1:8000

# --- Vercel / production “shell” when the live backend is Streamlit (no JSON API there) ---
# Omit NEXT_PUBLIC_API_BASE in the Vercel dashboard (do not set it), then set:
# NEXT_PUBLIC_STREAMLIT_APP_URL=https://milestone1-zomato-o.streamlit.app

# --- Later: hosted FastAPI + Next on Vercel ---
# NEXT_PUBLIC_API_BASE=https://your-api.example.com
# (leave NEXT_PUBLIC_STREAMLIT_APP_URL unset, or keep it for an extra “open Streamlit” link if you add one)
